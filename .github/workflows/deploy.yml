name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # Cambia a 'master' si esa es tu rama principal

# Añadir concurrencia para cancelar ejecuciones anteriores en la misma rama
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Usa la versión de Node.js que prefieras

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
        env:
          # ¡CORRECCIÓN CRÍTICA!
          # Los nombres de los secrets deben coincidir con los que configuraste en GitHub.
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist # Directorio de salida de Vite
          # Si tu repositorio usa una rama diferente para el despliegue (ej: 'gh-pages'), configúrala aquí:
          # publish_branch: gh-pages